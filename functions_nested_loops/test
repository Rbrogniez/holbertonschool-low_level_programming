/**
 * main - check the code.
 *
 * Return: Always 0.
 */

#include <stdio.h>

int _islower (int c)
{
	char ch;
	prinf("c");
	scanf("%c",&ch);

	if(ch >= "A" && ch <= "Z")
		printf("'%c'0",ch);
	else (ch >= "a" && ch <= "z")
		     return 0;
}
