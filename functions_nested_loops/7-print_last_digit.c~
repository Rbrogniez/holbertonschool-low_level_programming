#include "main.h"
/**
*print_last_digit - print last digit of x
*@x: element
*Return: always 0
*/
int print_last_digit(int x)
{
	int lastdigit;
	lastdigit = x % 10;
	_putchar ('0'+lastdigit);
	return (lastdigit);
}
